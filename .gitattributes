# Set the default behavior, in case people don't have core.autocrlf set.
* text=auto

# Explicitly declare text files you want to always be LF in the repo.
*.cpp   text eol=lf
*.h     text eol=lf
*.json  text eol=lf
*.md    text eol=lf
*.txt   text eol=lf

# Mark binary files as binary so Git leaves them alone.
*.png   binary
*.jpg   binary
